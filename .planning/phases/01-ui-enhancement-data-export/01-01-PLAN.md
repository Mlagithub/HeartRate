---
phase: 01-ui-enhancement-data-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app.css
  - src/lib/components/HeartRateChart.svelte
  - src/lib/components/HeartRateDisplay.svelte
autonomous: true
requirements:
  - UI-01
  - UI-02
user_setup: []

must_haves:
  truths:
    - "User sees a teal/green color palette instead of blue when viewing the app"
    - "User sees increased padding and whitespace throughout the interface"
    - "User sees a dark theme by default with reduced eye strain"
    - "Heart rate chart uses teal for the line instead of blue"
  artifacts:
    - path: "src/app.css"
      provides: "CSS variables for theme colors"
      contains: "--primary-color: #14b8a6"
      min_lines: 200
    - path: "src/lib/components/HeartRateChart.svelte"
      provides: "Chart visualization with updated colors"
      contains: "borderColor: '#14b8a6'"
    - path: "src/lib/components/HeartRateDisplay.svelte"
      provides: "BPM display with updated styling"
  key_links:
    - from: "src/app.css"
      to: "all components"
      via: "CSS variables (--primary-color, etc.)"
      pattern: "var\\(--primary-color\\)"
    - from: "src/lib/components/HeartRateChart.svelte"
      to: "Chart.js configuration"
      via: "borderColor, backgroundColor"
      pattern: "#14b8a6"
---

<objective>
Transform the visual design from a blue tech-focused palette to a calming teal/green health-focused aesthetic. Increase padding and whitespace for a minimalist look. Ensure dark theme is the default.

Purpose: Create a health-appropriate visual experience that reduces eye strain during extended monitoring sessions and feels native to health monitoring context.
Output: Updated CSS variables, modified chart colors, refined spacing throughout the interface.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ui-enhancement-data-export/01-CONTEXT.md
@.planning/phases/01-ui-enhancement-data-export/01-RESEARCH.md

<interfaces>
<!-- Current CSS Variables (from src/app.css) -->
```css
:root {
  --primary-color: #3b82f6;      /* Blue - CHANGE TO TEAL */
  --primary-hover: #2563eb;
  --primary-light: rgba(59, 130, 246, 0.15);
  --glow-primary: 0 0 20px rgba(59, 130, 246, 0.3);
  /* Zone colors - KEEP EXISTING */
  --zone-rest: #94a3b8;
  --zone-fat-burn: #22c55e;
  --zone-cardio: #f59e0b;
  --zone-peak: #ef4444;
  --zone-extreme: #dc2626;
}
```

<!-- HeartRateChart.svelte current chart colors -->
```typescript
borderColor: '#3b82f6',  // CHANGE TO TEAL
backgroundColor: 'rgba(59, 130, 246, 0.1)',  // CHANGE TO TEAL
```

<!-- Existing patterns from AlertSettings.svelte for spacing -->
```css
.settings-section { padding: 24px; }
.section-header { margin-bottom: 20px; padding-bottom: 16px; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CSS variables for teal/green health palette</name>
  <files>src/app.css</files>
  <read_first>
    - src/app.css (to see current variable values)
  </read_first>
  <behavior>
    - CSS variables change from blue (#3b82f6) to teal (#14b8a6)
    - All primary color references use the new teal palette
    - Zone colors remain unchanged (already health-appropriate greens/yellows/reds)
    - Dark theme is default (no change needed - already default)
  </behavior>
  <action>
    Per D-01: Replace blue primary color with teal/green health-focused palette.

    In src/app.css, update the :root section:

    1. Change --primary-color from #3b82f6 to #14b8a6 (teal-500)
    2. Change --primary-hover from #2563eb to #0d9488 (teal-600)
    3. Change --primary-light from rgba(59, 130, 246, 0.15) to rgba(20, 184, 166, 0.15)
    4. Change --glow-primary from rgba(59, 130, 246, 0.3) to rgba(20, 184, 166, 0.3)

    Also update the [data-theme="light"] section:
    5. Change --glow-primary from rgba(59, 130, 246, 0.15) to rgba(20, 184, 166, 0.15)

    Per D-03: Increase spacing for minimalist aesthetic:
    6. Change .glass-card padding from implicit to explicit 24px
    7. Update button padding from "10px 20px" to "12px 24px"
    8. Add body line-height: 1.6 for better readability

    DO NOT modify zone colors (--zone-*) - these are already health-appropriate.
    DO NOT change font-family - Inter is retained per D-04.
  </action>
  <verify>
    <automated>grep -E "^\\s*--primary-color:\\s*#14b8a6" src/app.css && echo "Primary color updated" || echo "FAIL: Primary color not updated"</automated>
  </verify>
  <done>
    - src/app.css contains --primary-color: #14b8a6
    - src/app.css contains --primary-hover: #0d9488
    - src/app.css contains --glow-primary with rgba(20, 184, 166
    - All blue color references replaced with teal equivalents
  </done>
</task>

<task type="auto">
  <name>Task 2: Update chart colors to use teal palette</name>
  <files>src/lib/components/HeartRateChart.svelte</files>
  <read_first>
    - src/lib/components/HeartRateChart.svelte (to see current chart config)
    - src/app.css (to reference new CSS variables)
  </read_first>
  <behavior>
    - Chart line uses teal instead of blue
    - Chart fill uses teal with transparency
    - Hover point uses teal
    - Zone colors unchanged (already appropriate)
  </behavior>
  <action>
    Per D-01: Update HeartRateChart.svelte to use the new teal color scheme.

    In the chart configuration (initChart function):

    1. Change borderColor from '#3b82f6' to '#14b8a6'
    2. Change backgroundColor from 'rgba(59, 130, 246, 0.1)' to 'rgba(20, 184, 166, 0.1)'
    3. Change pointHoverBackgroundColor from '#3b82f6' to '#14b8a6'

    In the tooltip configuration:
    4. Keep existing dark tooltip colors (no change needed)

    In the plugin configuration:
    5. Zone colors in zoneColors array are already correct - DO NOT change them
    6. getBpmColor function is already correct - DO NOT change it

    The result should be a teal chart line that matches the new health-focused aesthetic.
  </action>
  <verify>
    <automated>grep -E "borderColor.*#14b8a6" src/lib/components/HeartRateChart.svelte && echo "Chart color updated" || echo "FAIL: Chart color not updated"</automated>
  </verify>
  <done>
    - HeartRateChart.svelte contains borderColor: '#14b8a6'
    - HeartRateChart.svelte contains backgroundColor with rgba(20, 184, 166
    - Chart renders with teal line instead of blue
  </done>
</task>

<task type="auto">
  <name>Task 3: Update HeartRateDisplay component spacing and styling</name>
  <files>src/lib/components/HeartRateDisplay.svelte</files>
  <read_first>
    - src/lib/components/HeartRateDisplay.svelte (to see current styling)
    - src/app.css (to reference new CSS variables)
  </read_first>
  <behavior>
    - Component uses increased padding for minimalist look
    - Colors use CSS variables for consistency
    - Text styling updated for better hierarchy
  </behavior>
  <action>
    Per D-03: Increase padding and whitespace for minimalist health aesthetic.

    Read HeartRateDisplay.svelte and update styling:

    1. If component has inline styles, increase padding values by approximately 20%
    2. Ensure BPM display uses CSS variable --primary-color instead of hardcoded blue
    3. Add margin-bottom: 4px to labels for better spacing
    4. If font-weight is used, ensure hierarchy with 600 for labels, 700 for values

    The component should feel more spacious with clearer visual hierarchy.
  </action>
  <verify>
    <automated>test -f src/lib/components/HeartRateDisplay.svelte && echo "HeartRateDisplay exists" || echo "FAIL: HeartRateDisplay not found"</automated>
  </verify>
  <done>
    - HeartRateDisplay.svelte uses CSS variables for colors
    - Component has improved spacing
    - Visual hierarchy is clear with appropriate font weights
  </done>
</task>

</tasks>

<verification>
1. Run the app and verify teal color appears instead of blue throughout
2. Check chart renders with teal line and teal fill
3. Verify zone colors remain unchanged (green/yellow/red)
4. Verify spacing feels more generous in all components
</verification>

<success_criteria>
- CSS variables define teal primary color (#14b8a6)
- Chart uses teal for line color
- All components reference CSS variables for theming
- Zone colors preserved (health-appropriate)
- Dark theme remains default
</success_criteria>

<output>
After completion, create `.planning/phases/01-ui-enhancement-data-export/01-01-SUMMARY.md`
</output>